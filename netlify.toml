[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single HTML entry — send everything to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
